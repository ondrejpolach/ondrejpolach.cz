query Posts($id: ID!)  {
    post(id: $id) {
        id,
        image {
            url
        },
        published_at,
        name,
        content,
        tags {
          name
        }
    }
}