query Posts($tags: [String]) {
    posts(where: {tags: { name: $tags}}) {
        id,
        image {
            url
        },
        published_at,
        name,        
        content,
        description
        tags {
          name
        }
    }
}