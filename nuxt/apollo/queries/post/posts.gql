query Posts($tags: [String]) {
    posts(where: {tags: { name: $tags}}) {
        id,
        imageUrl,
        published_at,
        name,        
        content,
        description
        tags {
          name
        }
    }
}